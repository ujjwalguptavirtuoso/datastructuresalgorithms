package sorting;
import java.util.*;
/**
 * ujjwal.gupta
 *
 * @version $Id: QuickSort.java, v 0.1 2022-05-11
 */
public class QuickSort {

    int partition(int [] nums, int low, int high){
        return -1;
    }

    int randomPivotAlgo(int [] nums, int low, int high){
       return low + (int)(Math.random() * ((high - low) + 1)); // random pivot in range low, high
    }

    int quickSort(int [] nums, int low, int high){
        if(low<high){

        }

        return -1;
    }
}
